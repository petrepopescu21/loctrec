# Auth Service — Local Development Environment
# Copy to .env and fill in values: cp .env.example .env

# Server
PORT=8081

# Database
DATABASE_URL=postgres://loctrec:loctrec@localhost:5432/loctrec?sslmode=disable

# JWT Keys (ES256)
# Leave empty to auto-generate ephemeral keys (fine for local dev).
# For stable keys across restarts, generate a key pair:
#   openssl ecparam -name prime256v1 -genkey -noout -out private.pem
#   openssl ec -in private.pem -pubout -out public.pem
# Then paste the PEM contents here (use \n for newlines):
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=

# Google OAuth2
# Create credentials at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8081/oauth/google/callback
