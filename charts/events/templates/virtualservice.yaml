apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Chart.Name }}
spec:
  hosts:
    - "*"
  gateways:
    - loctrec-gateway
  http:
    - match:
        - uri:
            prefix: /api/events
      rewrite:
        uri: /
      route:
        - destination:
            host: {{ .Chart.Name }}
            port:
              number: {{ .Values.service.port }}
